apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: emailservice-worker-scaledobject
  namespace: workers
spec:
  scaleTargetRef:
    name: emailservice-worker
  pollingInterval: 10
  cooldownPeriod: 60
  minReplicaCount: 0
  maxReplicaCount: 8
  triggers:
    - type: azure-servicebus
      metadata:
        namespace: sre-sb-namespace
        topicName: business-events
        subscriptionName: emailservice-sub
        messageCount: "5"
      authenticationRef:
        name: emailservice-worker-trigger-auth
