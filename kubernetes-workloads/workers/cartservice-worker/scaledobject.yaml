apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: cartservice-worker-scaledobject
  namespace: workers
spec:
  scaleTargetRef:
    name: cartservice-worker
  pollingInterval: 10
  cooldownPeriod: 60
  minReplicaCount: 0
  maxReplicaCount: 10
  triggers:
    - type: azure-servicebus
      metadata:
        namespace: sre-sb-namespace
        topicName: business-events
        subscriptionName: cartservice-sub
        messageCount: "5"
      authenticationRef:
        name: cartservice-worker-trigger-auth
