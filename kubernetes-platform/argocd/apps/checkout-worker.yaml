apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: checkout-worker
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/jaeveloper/sre-proj-upgrade
    targetRevision: main
    path: helm/workers/checkout-worker

  destination:
    server: https://kubernetes.default.svc
    namespace: workers

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
